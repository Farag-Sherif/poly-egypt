# PolyEgy React + Vite

This project has been migrated to a pure React + Vite application while preserving the existing UI, routes, API integrations, and behavior.

## Available scripts

- `npm run dev`
- `npm run build`
- `npm run preview`
